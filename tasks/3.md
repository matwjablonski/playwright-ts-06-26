<!-- _class: time20 -->

## Zadanie nr 3

**TypeScript: modelowanie danych TodoLab**

W `src/types.ts`:

- dodaj typ `TodoOwner`
- dodaj opcjonalne pole `owner?: TodoOwner` do `Todo`
- dodaj typ `TodoImportState = 'success' | 'empty' | 'error'`
- opisz priorytet jako union type, nie jako dowolny `number`

- zaktualizuj `public/data.json`
- pokaż `owner` na karcie zadania
- uruchom `npm run build`
