<!-- _class: time30 -->

## Zadanie nr 16

Przetestuj routing i autoryzację:

- wejście na `/#/todos` bez sesji przekierowuje na `/#/login`
- błędne hasło pokazuje `Nieprawidłowe hasło.`
- poprawne hasło przekierowuje na `/#/todos`
- link `O aplikacji` zmienia URL na `/#/about`
- `Wyloguj` czyści sesję i wraca do logowania

Przetestuj funkcje plikowe:

- import `public/import-example.json` przez pole `Import zadań z JSON`
- eksport przez przycisk `Eksportuj JSON` i `page.waitForEvent('download')`
- popup raportu przez `page.waitForEvent('popup')`
