<!-- _class: time25 -->

## Zadanie nr 12

- opakuj dłuższy test w `test.step`, np. `Log in`, `Add todo`, `Verify list`
- uruchom testy i otwórz raport:

```sh
npm test
npx playwright show-report
```

- skonfiguruj raportery: `list`, `html`, `json`
- ustaw `screenshot: 'only-on-failure'`, `video: 'retain-on-failure'`, `trace: 'retain-on-failure'`
- celowo zepsuj jedną asercję i sprawdź artefakty w raporcie
- uruchom `npx playwright test --debug`
- dodaj tymczasowo `await page.pause()` i sprawdź Playwright Inspector
