<!-- _class: time20 -->

## Zadanie nr 8

**Page Object Pattern**

Stwórz `tests/pages/TodosPage.ts`.

Dodaj metody:

```ts
async goto(): Promise<void>
async addTodo(task: string, priority: '1' | '2' | '3'): Promise<void>
async getTodoCount(): Promise<number>
async removeTodoByName(task: string): Promise<void>
```

Przepisz test dodawania zadania tak, aby test opisywał scenariusz, a szczegóły locatorów były w Page Object.
